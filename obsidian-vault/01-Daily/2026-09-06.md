---
type: daily
date: 2026-09-06
tags: [daily, airtouch, gesture, accessibility, calibration, agents, harness]
---

# 2026-09-06

## やったこと（1）エアタッチのブラッシュアップ
- エアタッチ（`assets/js/gesture-pointer.js`）をブラッシュアップした
  （ブランチ `claude/zero-1-gesture-control-68dq1u`）。深澤の要望は4方向すべて:
  - **狙いやすさ**: つまみ方の個人差を実測する調整（`PinchCalibration` / `deriveThresholds`）と、
    押し切る前の「あと少し」を見せる連続表示（`pinchProgress` → `--airtouch-progress`）
  - **疲れにくさ**: 静止クリック（`dwell`・既定OFF）と、速さでスクロール倍率を変える `scrollGain`（最大3.2倍）
  - **できることを増やす**: 中指つまみ＝右クリック（`contextmenu`）、横払いで戻る（既定OFF）
  - **見た目と情報**: できることの一覧（起動時に一度出す）、プレビューと案内の表示位置切替
- 設定の保存（`SETTINGS_KEY` / localStorage）を足した。調整を毎回やり直させない
- 検査 57 → **84項目**（`scripts/verify-gesture-pointer.mjs`）。故障注入8件すべてで落ちることを確認

### 学んだこと・気づき（エアタッチ）
- **「押しても反応しない」の正体は、たいてい閾値が本人に合っていないこと**。
  手の大きさで正規化してもなお個人差が残る。実測して2つの山の間へ線を引くしかない。
  ただし**山が重なっていたら閾値を作らない**——無理に作ると「調整したのに前より当たらない」になる
- **指は独立に動かない**。人差し指と親指を摘まむと、中指も一緒に親指へ寄る。
  中指つまみだけを見て右クリックにすると、**普通のタップのたびにメニューが出る**。
  「人差し指が開いている」を条件に足して初めて区別できる
- **検査が「何を数えるか」を間違えると、故障注入をすり抜ける**。調整中の押下漏れを
  クリック数で見ていたが、調整中は指を離さないので click は元々0件。押下を素通しに
  戻しても検査が通った。**押し始め（pointerdown）を数える**のが正しい
  → [[verification-counts-the-wrong-thing]]（検査が数える対象を間違えると故障注入をすり抜ける）
- **既定でONにしてよい機能と、そうでない機能がある**。静止クリック（止めるだけで押す）と
  横払いで戻るは、便利さより誤爆の被害が大きい（後者は書きかけの入力が消える）。
  選んだ人だけに効かせる
- **切ってある操作を一覧に載せない**。書いてあるのに反応しないと「壊れている」と受け取られる。
  逆に、**何ができるか分からないまま手をかざしても何も起きず**、それも「動かない」と判断される。
  起動時に一度だけ出すのが折衷点

### 次にやること（エアタッチ）
- 実機（深澤のスマホ／PC）で調整を通して、閾値の分布が想定どおりか確かめる
- PC版 ZERO-1（別リポジトリ `zero-1-local-ai`）の `npm run compare` 結果待ち → 既定モデルの決定

## やったこと（2）全AIエージェントのブラッシュアップ
- 全AIエージェントのブラッシュアップ（深澤依頼）。19体 → 22体。
  - 共通運用規約 `.claude/agent-conventions.md` を新設（契約書式・最小権限・必須検査の対応表・停止条件・振り分け）
  - 全22体に `tools:` を宣言。診断/監査系から Edit・Write を外した
  - 全体に「契約（受け取る／返す／次の担当）」「必須検査」「停止条件」を追加。旧「入力」節は削除
  - 新設: `asset-guardian`（品質ゲート4体目）／`triage`（切り分け）／`verifier`（検査の作成）
  - harness-lint 検査#14 を追加（規約適合の機械検査）。故障注入で✗を確認
- 旧ファイル名 `game.html` の残存6件を `zelda_like.html` へ修正。`security.md` の現存しない構成説明を訂正
- `dynamic-tester.md` に貼ってあった検証スクリプトの古い写し81行を削除（「写しを編集して使うな」と書いた直後に写しがあった）
- 「19体」の記載を note記事・topics.json・SNS文面・`post-social.js`・Instagram画像（ig-04）まで同期

### 学び（エージェント）
- **文章の禁止事項は破られる。権限で表現できるものは権限で表現する**（tools の最小化）
- **規約は検査とセットで入れる**。検査#14は導入直後に自分の書き漏らし2件を捕まえた
- **数字は機械検査が無いと黙って嘘になる** — エージェント数の変更が記事・投稿・画像へ波及した

## やったこと（3）必須検査 verify-game-assets の偽の赤を潰す

- **静的検査の29件の偽FAIL**（源平争乱記）を修正。解決先を「そのファイル自身が宣言している
  `assets/.../`」に限って追加する形にした（推測で広げない＝偽の緑へ裏返らせない）
- **別オリジン（CDN・Webフォント）の失敗をFAILから分離**（`△ 外部` 表示のみ）。
  `dynamic-test-auto.cjs` が先に持っていた設計をこちらへ横展開
- **検査自身のクリックで始まった遷移の中断**を404と呼んでいた3つ目の偽FAILも修正
  （`card-games.html` → 実在する `poker.html`）。除外は**実体が在るときだけ**に限定
- 故障注入5パターン（綴り間違い／どこにも無いパス／実在しないローカル画像／ページ例外／
  実在しないページへのリンク）すべてで✗を確認

### 学び（検査）
- **偽の赤は検査が無いより悪い**。常に赤い検査は「見なくてよい理由」を与える
  → [[false-red-checks-are-worse-than-none]]
- 「必須」と書いた検査が赤いまま放置されていたら、**まず検査を疑う**

### 決定
- [[0034-agent-contract-and-least-privilege]]

## やったこと（4）偽の赤を消したら、本物の不具合が2件出てきた

全95ページを通したところ、96 ok / 2ページFAIL。**ノイズを消して初めて見えた本物**だった。

- **`hyakki.html`**: `` img.src = `assets/hyakki-char-${i}.png` `` — 実体は `.webp`。
  WebP移行のとき、**実行時に組み立てるパス**（CLAUDE.mdが「一括置換で直らない」と警告している形）
  だけ取り残されていた。すぐ下の `actionImages` は `.webp` になっており、**片方だけ直っていた**。
  キャラ画像5枚が読めないまま、例外も出ずに表示されていなかった
- **`notebook.html`**: ①favicon が `assets/favicon.svg`（実体は `assets/logo/favicon.svg`。他11ページは正しい）
  ②**CDN（marked）が読めないと `init()` の1行目で ReferenceError → ページ全体が無反応**。
  資料一覧も保存済みの結果も出ない。整形ができないだけのはずが、全部が死んでいた
  → `renderMarkdownInto()` でフォールバック（`textContent`・XSS経路を作らない）＋理由を画面に出す。
  CDNあり／なしの両経路をヘッドレスで実測（XSSペイロードが実行されないことも確認）
- **`release-check` のSRI検査が gtag.js に偽の✗**を出していた（SRI適用不可・15ページが同じスニペット）。
  除外に理由付きで追加し、**本物のSRI無しスクリプトでは今も✗になる**ことを故障注入で確認

### 深澤へ相談したいこと（未対応）
- `notebook.html` の `marked` は**バージョン未固定なのに integrity 付き**。jsdelivr が新版を配ると
  ハッシュ不一致で**ブラウザが黙ってブロック**する時限式の不具合。固定には実際のハッシュ実測が要る
  （この環境はjsdelivrへ出られないため未対応）
- 15ページの gtag が **`G-XXXXXXXXXX` のまま**（プレースホルダ）。計測されないリクエストを毎回出している。
  設定するか外すかは判断が要る

## 昇格済み（どこへ反映したか）

- エアタッチの学び5件 → **反映済み**: CLAUDE.md「ZERO-1 Mobile とエアタッチの必須チェック」節
  （閾値の個人差・調整中は押下を通さない・中指つまみの条件・静止クリック既定OFF・設定の保存）と
  [[verification-counts-the-wrong-thing]]
- エージェント刷新の学び3件 → **反映済み**: `.claude/agent-conventions.md`（共通規約）／
  全22体の定義（`tools:` 最小化・契約・必須検査・停止条件）／harness-lint 検査#14 ／
  [[0034-agent-contract-and-least-privilege]]
- 検査の学び2件 → **反映済み**: CLAUDE.md「画像アセットの方針」節（静的検査の解決先・
  別オリジンの分離）／`scripts/verify-game-assets.mjs` ／ [[false-red-checks-are-worse-than-none]]
- 「偽の赤を消すと本物が出る」実例（hyakki の取り残し・notebook の全面停止）→ **反映済み**:
  [[false-red-checks-are-worse-than-none]] に追記／`release-check` のSRI除外（理由付き）
